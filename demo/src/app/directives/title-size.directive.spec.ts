import { TitleSizeDirective } from './title-size.directive';

describe('TitleSizeDirective', () => {
  it('should create an instance', () => {
    //const directive = new TitleSizeDirective();
    //expect(directive).toBeTruthy();
  });
});
